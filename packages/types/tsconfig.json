{
  "extends": "../../tsconfig.json",
  "include": ["**/*"],
  "exclude": ["node_modules", "dist"],

  "compilerOptions": {
    // package-specific options
    "composite": true,
    "rootDir": "./src",
    "baseUrl": "./src"
  }
}
