import { Job } from "types";

/** Schedules the next job run into the scheduler table */
export const scheduleNext = (job: Job) => {
  // todo
};

/** Removes a job from the scheduler table */
export const unschedule = (job: Job) => {
  // todo
};
